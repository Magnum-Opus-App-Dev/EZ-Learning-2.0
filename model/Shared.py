class Share:
  def __init__(self, shareId, folderId, sharedBy, sharedTo, status):

    self.shareId = shareId
    self.folderId = folderId
    self.sharedBy = sharedBy
    self.sharedTo = sharedTo
    self.status = status